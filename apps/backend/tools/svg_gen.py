"""SVGGenerator — genera SVG bundle per Cricut/Silhouette. [STUB — non implementato in Fase 2B]"""

from __future__ import annotations

from pathlib import Path


class SVGGenerator:
    """Genera SVG bundle per Cricut/Silhouette. [STUB — non implementato in Fase 2B]"""

    async def generate_bundle(self, brief: dict, output_dir: Path) -> list[Path]:
        raise NotImplementedError("SVG generator sarà implementato in Fase 2C")
